{
  "name": "kardoor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "nuxt"
  ],
  "scripts": {
    "dev": "npm --prefix nuxt run dev",
    "build": "npm --prefix nuxt run build",
    "generate": "npm --prefix nuxt run generate",
    "preview": "npm --prefix nuxt run preview",
    "typecheck": "npm --prefix nuxt run typecheck",
    "check": "npm --prefix nuxt run typecheck"
  },
  "dependencies": {
    "nuxt": "^4.4.6"
  }
}
